<h1>Posts#show</h1>
  <h1><%= @post.title %></h1>
  <p><%= @post.content %></p>
  <%= button_to '❤️', like_path(@post.id)  %>
  <p>  Likes: <%= @post.likes %></p>

  <%= link_to @blogger.name, blogger_path(@blogger.id) %>
  <%= link_to @destination.name, destination_path(@destination.id) %>
